# {需求名称} - PRD分析报告

## 1. 需求概览
| 属性 | 内容 |
|------|------|
| 需求名称 | {name} |
| 需求编号/版本 | {version} |
| 分析日期 | {date} |
| PRD 文件路径 | {prd_path} |
| 目标用户/角色 | {roles} |

## 2. 功能模块清单

### 2.1 功能概览
- **核心功能**: 简述该需求解决的核心问题或提供的核心能力
- **目标用户**: 明确谁会使用这个功能（例如：普通消费者、后台客服人员、系统管理员），用于确定测试时的身份视角
- **业务场景**: 描述该功能在什么情况下被使用

### 2.2 页面结构与交互流
按系统模块=>页面=>页面模块/组件进行划分，为每个页面分配编号`{SystemModule}-{PageCode}`，示例：
  `TRADE-LIST`: 交易列表页
  `TRADE-CREATE`: 创建交易页
  `TRADE-DETAIL`: 订单详情页

#### 页面1: {page_name}
- **URL**：如果是web、H5页面的话
- **页面入口**: 说明用户从哪个路径进入该页面（例如：从“个人中心”点击“我的订单”进入，或通过站内信跳转）。
- **主要功能**: 罗列该页面承载的关键操作（例如：信息展示、表单输入、点击提交按钮、筛选过滤）。
- **页面跳转**: 描述操作后可能的其他页面流向

---

**注**: 对于包含3个及以上页面的复杂模块，请补充以下子章节：

#### 2.2.1 页面功能层级关系图
使用树状结构展示页面与该页面的核心功能列表层级关系
```
{page_hierarchy_diagram}
示例：
交易系统
├── 道具价值列表页 (Value List)
│   └── 展示所有道具的当前价值和交易热度
├── 交易列表页 (Trade List)  
│   └── 展示所有活跃的交易订单
├── 创建交易页 (Create Trade)
│   ├── 指定交易模式
│   └── 开放报价模式
└── 订单详情页 (Order Detail)
    ├── 查看订单详情
    ├── 接受交易(指定模式)
    ├── 提交报价(开放模式)
    └── 查看报价列表(开放模式)
```

#### 2.2.2 页面功能矩阵
描述页面名称、URL、访问的点击路径、核心功能、主要用户操作、跳转的目标页面
| 页面名称 | 访问路径 | URL | 核心功能 | 主要用户操作 | 跳转目标页面 |
|---------|---------|-----|---------|-------------|-------------|
| [页面1] | [路径] | [URL地址] | [功能] | [操作1]、[操作2] | [目标页面] |
| [页面2] | [路径] | [URL地址] | [功能] | [操作1]、[操作2] | [目标页面] |

#### 2.2.3 页面跳转关系图
页面之间的跳转导航关系图
```
示例：
[列表页] --点击行--> [详情页]
[详情页] --点击编辑--> [编辑页]
[编辑页] --保存成功--> [详情页]
[编辑页] --取消--> [详情页]
```

## 3. 组件级交互说明
- 每个页面的组件分配编号：`{SystemModule}-{PageCode}-{ComponentCode}`
- *按照页面从上到下、从左到右的视觉顺序描述页面模块和组件*
- 重点关注视觉和交互逻辑
- 每个UI元素的描述必须包含：视觉特征 + 交互逻辑与状态转换
- **[页面区域/模块名称]**
  - **[组件名称/区块]**
    - **功能描述**：该组件的主要用途
    - **视觉特征**：
      - 整体布局与外观：容器样式、排列方式、响应式变化（如有）
      - 子元素清单及其静态视觉，如果发现组件下的子元素逻辑非常复杂，建议拆分为独立的组件
    - 组件状态枚举（所有可能的外观状态）
      - 交互状态：默认、hover、active、focus、disabled、loading 等
      - 业务状态：选中/未选中、展开/收起、有数据/空数据、错误态、成功态、不同变体（primary, danger等）
      - 条件渲染状态：登录前/后、权限有无、数据是否为空等
    - **交互逻辑与状态转换**：
      - 触发事件（点击、悬停、键盘、拖拽等）
      - 行为描述（点击后发生什么，包括调接口、显示/隐藏其他元素、防抖等）
      - 状态流转路径（如 default → hover → active → loading → success/error → default）
      - 键盘交互细节（Tab 序、Enter/Esc 等）
    - 子元素联动与全局状态组合
      - 不同全局状态下各子元素的组合显示（如未登录显示 A，已登录显示 B）
      - 一个子元素的操作如何影响另一个子元素（如搜索框聚焦时屏蔽某些按钮）
      - 多层弹窗/面板的互斥与关闭逻辑
    - **边界与异常场景**
      - 输入或系统状态处于临界边缘（如空值、极值、零、最大/最小长度、业务逻辑数值临界点）时的表现场景
      - 文本超长、数字溢出、特殊字符
      - 网络错误、接口超时、数据格式异常时的表现
      - 极快连续操作（连点、频繁切换）的预期行为
      - 非法参数或缺失必要 props 时的降级渲染
示例：
```
### 3.1 TRADE-LIST (交易列表页)

#### 3.1.1 TRADE-LIST-FILTER (筛选区域)
- **编号**: TRADE-LIST-FILTER
- **功能描述**: 用户可按类型、状态、时间筛选交易订单
- **视觉特征**: 顶部横向排列的下拉框和日期选择器
- **交互逻辑**:
  - 触发条件: 用户需要缩小订单范围
  - 用户操作: 选择筛选条件后点击"搜索"按钮
  - 即时反馈: 按钮显示 loading 状态
  - 状态变化: 列表区域刷新
  - 后续影响: 触发 `TRADE-LIST-TABLE` 数据更新
- **数据状态影响**: 
  - 无筛选结果时显示空状态提示
- **业务规则**: 
  - 时间范围不得超过90天

#### 3.1.2 TRADE-LIST-TABLE (订单列表)
- **编号**: TRADE-LIST-TABLE
- **功能描述**: 展示符合条件的交易订单
- ...
```

## 4. 核心业务流程

- 为流程分配编号：{SystemModule}-FLOW-{FlowName}，并包含以下基本信息
- **[流程名称]**（如：创建交易）
  1. **触发入口**：用户从哪里开始此流程
  2. **步骤分解**：每个步骤的用户操作和系统反馈
  3. **验证规则**：各步骤的数据验证和错误处理
  4. **完成状态**：流程完成后的结果和后续操作
示例：
```
### 4.1 TRADE-FLOW-CREATE (创建交易流程)
- **流程编号**: TRADE-FLOW-CREATE
- **触发入口**: 从 `TRADE-LIST` 点击"创建交易"按钮
- **步骤分解**:
  1. 进入 `TRADE-CREATE` 页面
  2. 填写 `TRADE-CREATE-FORM` 表单
  3. 点击 `TRADE-CREATE-SUBMIT` 按钮
  4. 跳转至 `TRADE-DETAIL` 查看新订单
- **验证规则**: 
  - 表单字段验证规则（参考 `TRADE-CREATE-FORM` 组件说明）
- **完成状态**: 订单创建成功，状态为"待接受"
```

## 5. 测试关注重点

- **核心业务流程**：验证产品最主要的用户路径是否畅通以及是否满足PRD定义的核心功能目标
- **边界条件**：针对输入框、数值范围、时间区间、分页等临界值的检查。如：最大字符长度、数值的最小值/最大值、列表翻页的最后一页等，这是最容易出现逻辑错误（如大于还是大于等于）的地方

**重点关注以上两点**，以下几点根据不同需求而定作为补充
- 异常/错误场景：验证系统在非正常操作或环境下的健壮性。如：网络错误、接口错误、前端操作异常、系统异常等
- 权限控制：登录态和不同角色权限在的交互差异性
- 兼容性要求（如 PRD 提及）：确保产品在不同软硬件环境下的表现一致。如响应式和多端差异
- 性能/安全要求（如 PRD 提及）：针对非功能性需求的专项验证。

## 6. 涉及模块清单
| 模块名称 | 影响类型 | 说明 |
|----------|----------|------|
| {module_name} | 新增/修改/关联 | {impact_reason} |

## 7. 术语说明
| 术语 | 说明 |
|------|------|
| {term} | {definition} |

## 8. 风险与待确认项
### 8.1 需求模糊点
- [需求模糊: {ambiguous_point}，建议确认: {confirmation_question}]

### 8.2 潜在冲突点
- {conflict}

### 8.3 可能遗漏场景
- {missing_scenario}

### 8.4 待补充资料
- {missing_artifact}

